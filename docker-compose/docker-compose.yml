version: "3.8"

services:
  uptime-kuma:
    image: louislam/uptime-kuma:latest
    container_name: uptime-kuma
    restart: unless-stopped
    ports:
      - "3001:3001"   # بورت الوصول للـ web app
    volumes:
      - ./data:/app/data   # تخزين البيانات بشكل دائم
